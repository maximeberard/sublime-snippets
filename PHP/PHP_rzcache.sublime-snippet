<snippet>
	<content><![CDATA[
\$cacheHash = "${1:Cache name}";
if (rz_cache::get(\$cacheHash)) {
	
	ob_start();

	$0

	rz_cache::set(\$cacheHash, ob_get_contents());
}
echo rz_cache::get(\$cacheHash);
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>rz_cache</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.php, text.php</scope>
</snippet>
